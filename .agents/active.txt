Active: server schema repair for promotions.max_uses (HTTP 500 on coupon APIs when DB behind migrations)
Done:
- app/db_config/schema_repair.py: ensure_promotions_max_uses() via SQLAlchemy inspect + ALTER TABLE (mysql/sqlite)
- app/factory.py: run repair inside app_context after models import
Next:
- Restart API (docker compose) and retest GET /api/admin/coupons; run `alembic upgrade head` in prod if preferred over auto-repair
