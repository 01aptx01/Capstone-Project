Admin header global search: เสร็จแล้ว — backend `q` บน `GET /api/admin/machines` + `GET /api/admin/orders` (SQL filter), swagger, `admin-api.ts` listMachines/listOrders, Header command palette (debounce 250ms, parallel API, ⌘K/Ctrl+K, Escape, คลิกนอกปิด), หน้า products/customers/orders/machines รองรับ `?q=` ผ่าน Suspense + client inner component.
Next (ถ้าต้องการ): ลูกศรเลือกรายการในแผงค้นหา; ลิงก์ `/machines?q=` จากเมนูแทนรายการตู้เดี่ยว.
