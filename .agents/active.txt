Admin-ui wired to Flask /api/admin (browser via NEXT_PUBLIC_ADMIN_API_URL): products, machines, orders, customers metrics, coupons. Next mock routes kept: dashboard, notifications, alerts, sales. Mock route handlers removed for products, machines, orders.
Next: optional admin auth, coupon create modal wiring, backend aggregates for coupon usage / order line counts.
