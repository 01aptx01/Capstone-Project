%% High-level deployment — README + docker-compose (logical, not every env override)

flowchart LR
    subgraph clients["Clients"]
        MU[machine-ui\nNext.js :3000]
        AU[admin-ui\nNext.js :3001]
    end

    subgraph backend["Backend"]
        API[Flask server\n:8000 REST + Socket.IO]
    end

    subgraph data["Data"]
        DB[(MySQL :3307)]
    end

    subgraph external["External"]
        OM[Omise API]
    end

    subgraph edge["Edge"]
        AG[Hardware Agent\nFlask :5000]
        HW[มอเตอร์ / NFC / LED]
    end

    MU -->|HTTPS JSON| API
    AU -->|HTTPS JSON| API
    API -->|SQL| DB
    API -->|HTTPS| OM
    API -->|HTTP /jobs/start หรือ Socket.IO| AG
    AG --> HW
    AG -->|POST /api/machines/events| API
