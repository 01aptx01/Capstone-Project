# Flask API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Machine for product catalog
NEXT_PUBLIC_MACHINE_CODE=MP1-001

# Omise public key
NEXT_PUBLIC_OMISE_PUBLIC_KEY=pkey_test_xxxxxxxx

# Must match server JWT_SECRET in repo root .env (not exposed to browser)
JWT_SECRET=dev-change-me-in-production
